export type User = {
  id: string;
  gitcoinScore: number;
  sismoContributorLevel: number;
  isSismoLensFollower?: boolean;
  githubId?: string;
  twitterId?: string;
  telegramId?: string;
  address?: string;
};
